variables:
  ml_ws_connection: 'edapmlops_aml' # Workspace Service Connection name
  ml_ws: 'edapmlopstest' # AML Workspace name
  ml_rg: 'dev-edap-ml-ops' # AML resource Group name
  ml_ct: 'cpu-cluster-1' # AML Compute cluster name
  ml_path: 'models/diabetes' # Model directory path in repo
  ml_exp: 'Exp_mlops_diabetes' # Experiment name
  ml_model_name: 'Model_diabetes' # Model name
  ml_aks_name: 'my-aks-9' # AKS cluster name
